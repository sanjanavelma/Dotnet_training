using System;
using System.Text.RegularExpressions;
class Program
{
    static void Main()
    {
        // RegexisMatch.re();
        // RegexMatch.res();
        // RegexMatches.ress();
        EmailCheck.EC();
        // string ins = "grapple apples a123e a!-@e";
        // string part = @"^a.*e";
        // MatchCollection m = Regex.Matches(ins, part);
        // foreach (Match v in m)
        // {
        //     Console.WriteLine(v.Value);
        // }
                
    }
}